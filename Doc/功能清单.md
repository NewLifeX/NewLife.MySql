# NewLife.MySql 功能清单

> 版本：v1.3 | 日期：2026-08-02
> 需求对应：[需求文档](需求文档.md) | 架构设计：[架构设计](架构设计.md)
> 基线：2026-08-01 代码审计基线（UnitTest 758 通过、EFCore 79×2 通过、EF6 135 通过）
> 历史：代码审计优化清单（D/P/A 项）已并入 [`Doc/代码审计.md`](代码审计.md)，本文档为项目完整功能清单。

> 状态标记：✅ 已完成 | 🟡 部分完成 | 🔧 规划中 | ⏸ 暂缓/占位 | ❌ 未开始 | ❓ 待确认 | — 不适用

## 拆分决策表

> 本项目为**纯后端 ADO.NET 类库**（无前端 UI、无 Web 管理后台），且跨平台差异（Unix Socket、压缩协议）作为同一驱动的统一特性实现，不按平台拆分。因此无前后端/多平台拆分，全部需求项为单一功能点，不写入拆分决策表。

| 需求项 | 原因 | 拆分结果 |
|--------|:----:|---------|
| （无拆分项） | — | — |

## SYS 系统基础

### 父需求：连接建立与认证

| 编码 | 功能 | 类型 | 实现 | 测试 | 注释 | 说明 |
|------|------|:----:|:----:|:----:|:----:|------|
| SYS-1 | 连接建立与握手 | 后端 | ✅ | ✅ | ✅ | `SqlClient.OpenAsync` 协议 V10 握手，`WelcomeMessage` 解析，`MySqlConnection` 生命周期 |
| SYS-2 | 多认证方式 | 后端 | ✅ | ✅ | ✅ | `Authentication`：mysql_native_password / caching_sha2_password / Auth Switch / RSA 公钥全量认证 |
| SYS-3 | WebAuthn 免密认证 | 后端 | ✅ | 🟡 | ✅ | `WebAuthnHandler`（FIDO2，MySQL 8.2+ authentication_webauthn_client），无专门测试 |

> 父需求状态：🟡（SYS-3 缺专门测试）

### 父需求：连接配置

| 编码 | 功能 | 类型 | 实现 | 测试 | 注释 | 说明 |
|------|------|:----:|:----:|:----:|:----:|------|
| SYS-4 | 连接字符串解析 | 后端 | ✅ | ✅ | ✅ | `MySqlConnectionStringBuilder`：Server/Port/Database/UserID/Password/超时/池参数/压缩/管道/重试等 20+ 参数及别名 |
| SYS-5 | SSL/TLS 加密连接 | 后端 | ✅ | ✅ | ✅ | `SslMode` None/Preferred/Required，TLS 1.2/1.3（net5+） |
| SYS-6 | Unix Socket 连接 | 后端 | ✅ | 🟡 | ✅ | 连接字符串 `Socket` 参数，.NET Core 5.0+ Linux/macOS 生效，无专门测试 |

> 父需求状态：🟡（SYS-6 缺专门测试）

### 父需求：连接池与生命周期

| 编码 | 功能 | 类型 | 实现 | 测试 | 注释 | 说明 |
|------|------|:----:|:----:|:----:|:----:|------|
| SYS-7 | 连接池管理 | 后端 | ✅ | ✅ | ✅ | `MySqlPool`/`MySqlPoolManager`：按连接字符串分池、Min/Max 池大小、存活期、空闲回收、服务器变量缓存 |
| SYS-8 | 连接健康检查 | 后端 | ✅ | ✅ | ✅ | `ConnectionHealth` 纯函数判定 + socket Poll，覆盖活跃/到龄/空闲验活 |
| SYS-9 | MySqlDataSource DI 集成 | 后端 | ✅ | ✅ | ✅ | `MySqlDataSource`（.NET 7+ DbDataSource），公共构造 + CreateCommand |

> 父需求状态：✅（所有子项均完成）

## NET 网络协议

### 父需求：协议与传输

| 编码 | 功能 | 类型 | 实现 | 测试 | 注释 | 说明 |
|------|------|:----:|:----:|:----:|:----:|------|
| NET-1 | MySQL 协议核心 | 后端 | ✅ | ✅ | ✅ | `SqlClient`：帧编解码、COM_QUERY/STMT_PREPARE/EXECUTE/INIT_DB、多结果集、断线重试（仅读语句） |
| NET-2 | 压缩协议 | 后端 | ✅ | ✅ | ✅ | zlib 压缩（`UseCompression`），`SendCompressedPacketAsync`/`ReadCompressedPacketAsync` 池化复用 |
| NET-4 | 读缓冲与零分配优化 | 后端 | ✅ | ✅ | ✅ | `ReadBuffer` 缓冲复用、`OwnerPacket`/`ArrayPool` 管理、OwnerPacketResourceTests 验证归还 |

> 父需求状态：✅（所有子项均完成）

### 父需求：Binlog 变更订阅

| 编码 | 功能 | 类型 | 实现 | 测试 | 注释 | 说明 |
|------|------|:----:|:----:|:----:|:----:|------|
| NET-3 | Binlog 变更订阅 | 后端 | ✅ | ✅ | ✅ | `BinlogClient`/`BinlogEvent`/`BinlogEventType`/`BinlogPosition`：订阅、过滤、自动重连、断点续传 |

> 父需求状态：✅（所有子项均完成）

## SQL 查询执行

### 父需求：查询接口

| 编码 | 功能 | 类型 | 实现 | 测试 | 注释 | 说明 |
|------|------|:----:|:----:|:----:|:----:|------|
| SQL-1 | 查询执行接口 | 后端 | ✅ | ✅ | ✅ | `MySqlCommand`：ExecuteReader/Scalar/NonQuery + Async 全链路真异步，`Cancel` 明确抛 NotSupportedException |
| SQL-2 | 参数化查询 | 后端 | ✅ | ✅ | ✅ | `@参数名` 客户端替换，自动转义防注入，跳过字符串字面量，全类型支持 |
| SQL-3 | 预编译语句 | 后端 | ✅ | ✅ | ✅ | `Prepare()` → COM_STMT_PREPARE/EXECUTE 二进制协议，`UseServerPrepare` 全局开关，DisposeAsync 异步关闭 |

> 父需求状态：✅（所有子项均完成）

### 父需求：数据类型

| 编码 | 功能 | 类型 | 实现 | 测试 | 注释 | 说明 |
|------|------|:----:|:----:|:----:|:----:|------|
| SQL-4 | 数据类型映射 | 后端 | ✅ | ✅ | ✅ | `MySqlFieldCodec` 文本/二进制协议编解码，`MySqlColumn`/`MySqlDbType` 完整类型映射，日期 InvariantCulture |
| SQL-5 | Geometry 空间类型 | 后端 | ✅ | ✅ | ✅ | `MySqlGeometry` 封装 WKB（OGC 标准），参数读写与 DataReader 类型映射 |

> 父需求状态：✅（所有子项均完成）

### 父需求：事务与存储过程

| 编码 | 功能 | 类型 | 实现 | 测试 | 注释 | 说明 |
|------|------|:----:|:----:|:----:|:----:|------|
| SQL-6 | 事务支持 | 后端 | ✅ | ✅ | ✅ | `MySqlTransaction`：四种隔离级别，Dispose 未提交自动回滚 |
| SQL-7 | 存储过程调用 | 后端 | ✅ | ✅ | ✅ | `CommandType.StoredProcedure`，IN/OUT 参数，SET+CALL+SELECT 多语句实现 |

> 父需求状态：✅（所有子项均完成）

### 父需求：结果集消费

| 编码 | 功能 | 类型 | 实现 | 测试 | 注释 | 说明 |
|------|------|:----:|:----:|:----:|:----:|------|
| SQL-8 | DataReader 增强 | 后端 | ✅ | ✅ | ✅ | `MySqlDataReader`：多结果集 NextResult、`ReadModelsAsync<T>` setter 委托缓存、BulkRead/ReadTableAsync 零拷贝 |
| SQL-9 | DataAdapter 支持 | 后端 | ✅ | ✅ | ✅ | `MySqlDataAdapter`：Fill DataTable/DataSet，`CreateDataAdapter` 工厂创建 |

> 父需求状态：✅（所有子项均完成）

## BATCH 批量操作

### 父需求：批量 DML

| 编码 | 功能 | 类型 | 实现 | 测试 | 注释 | 说明 |
|------|------|:----:|:----:|:----:|:----:|------|
| BATCH-1 | 字典参数集批量 | 后端 | ✅ | ✅ | ✅ | `ExecuteBatch`/Async：`IList<IDictionary<String,Object?>>`，自动 Prepare + Execute × N |
| BATCH-2 | 数组绑定批量 | 后端 | ✅ | ✅ | ✅ | `ExecuteArrayBatch`/Async：Oracle 风格数组绑定，参数 Value 按索引提取 |
| BATCH-3 | 管道化执行 | 后端 | ✅ | ✅ | ✅ | `Pipeline=true` 批发批收，BuildExecutePacket 与发送解耦，失败续读保持连接干净 |

> 父需求状态：✅（所有子项均完成）

### 父需求：批量导入

| 编码 | 功能 | 类型 | 实现 | 测试 | 注释 | 说明 |
|------|------|:----:|:----:|:----:|:----:|------|
| BATCH-4 | MySqlBulkCopy 批量导入 | 后端 | ✅ | ✅ | ✅ | DataTable/IDataReader → 表，基于 Pipeline+预编译+事务，RowsCopied 事件、列映射 |
| BATCH-5 | DbBatch API | 后端 | ✅ | ✅ | ✅ | `MySqlBatch`/`MySqlBatchCommand`/`MySqlBatchCommandCollection`（.NET 6+ DbBatch 标准） |

> 父需求状态：✅（所有子项均完成）

## META 元数据与兼容

### 父需求：元数据查询

| 编码 | 功能 | 类型 | 实现 | 测试 | 注释 | 说明 |
|------|------|:----:|:----:|:----:|:----:|------|
| META-1 | Schema 基础查询 | 后端 | ✅ | ✅ | ✅ | `SchemaProvider`：Databases/Tables/Columns/Indexes/DataTypes/ReservedWords 等 |
| META-2 | Schema 扩展查询 | 后端 | ✅ | ✅ | ✅ | Procedures/Views/Triggers/ForeignKeys/ForeignKey_Columns/Users 等额外元数据集合 |

> 父需求状态：✅（所有子项均完成）

### 父需求：兼容数据库

| 编码 | 功能 | 类型 | 实现 | 测试 | 注释 | 说明 |
|------|------|:----:|:----:|:----:|:----:|------|
| META-3 | 兼容数据库检测 | 后端 | ✅ | ✅ | ✅ | `DatabaseType`：握手版本串自动识别 MySQL/OceanBase/TiDB/Doris/Aurora/CloudSQL/MariaDB |

> 父需求状态：✅（所有子项均完成）

## EF 框架集成

### 父需求：XCode 集成

| 编码 | 功能 | 类型 | 实现 | 测试 | 注释 | 说明 |
|------|------|:----:|:----:|:----:|:----:|------|
| EF-1 | XCode ORM 集成 | 后端 | ✅ | ✅ | ✅ | `MySqlClientFactory` 继承 DbProviderFactory，XCode DAL 自动识别 MySQL 驱动 |

> 父需求状态：✅（所有子项均完成）

### 父需求：EF 集成

| 编码 | 功能 | 类型 | 实现 | 测试 | 注释 | 说明 |
|------|------|:----:|:----:|:----:|:----:|------|
| EF-2 | EF Core Provider | 后端 | ✅ | ✅ | ✅ | `NewLife.MySql.EntityFrameworkCore`：UseMySql、类型映射、LINQ 翻译、迁移、AUTO_INCREMENT |
| EF-3 | EF6 Provider | 后端 | ✅ | ✅ | ✅ | `NewLife.MySql.EntityFramework`：MySqlEFConfiguration/ProviderServices/MigrationSqlGenerator 等 |

> 父需求状态：✅（所有子项均完成）

## 待确认

| 编码 | 功能 | 问题 | 建议 |
|------|------|------|------|
| SYS-3 | WebAuthn 免密认证 | 无专门测试文件，仅实现 | 补充 WebAuthnHandler 单元测试 |
| SYS-6 | Unix Socket 连接 | 无专门测试文件（依赖真实 Linux/macOS 环境） | 视环境补充或标注平台限制 |
| — | 压缩协议 zstd | PackageTags 声明支持 zlib/zstd，但源码仅实现 zlib | 确认 zstd 是否规划中 |

## 统计

### 子功能统计

| 模块 | 标识 | 功能点数 | 实现完成 | 测试通过 | 注释覆盖 | 综合完成率 |
|------|------|:-------:|:--------:|:--------:|:--------:|:---------:|
| 系统基础 | SYS | 9 | 9 | 7 | 9 | 78% |
| 网络协议 | NET | 4 | 4 | 4 | 4 | 100% |
| 查询执行 | SQL | 9 | 9 | 9 | 9 | 100% |
| 批量操作 | BATCH | 5 | 5 | 5 | 5 | 100% |
| 元数据与兼容 | META | 3 | 3 | 3 | 3 | 100% |
| 框架集成 | EF | 3 | 3 | 3 | 3 | 100% |
| **总计** | **6** | **33** | **33** | **31** | **33** | **94%** |

### 父需求完成度

| 父需求 | 子项数 | 已完成 | 状态 | 说明 |
|--------|:-----:|:-----:|:----:|------|
| 连接建立与认证 | 3 | 2/3 | 🟡 | SYS-3 缺专门测试 |
| 连接配置 | 3 | 2/3 | 🟡 | SYS-6 缺专门测试 |
| 连接池与生命周期 | 3 | 3/3 | ✅ | 全部完成 |
| 协议与传输 | 3 | 3/3 | ✅ | 全部完成 |
| Binlog 变更订阅 | 1 | 1/1 | ✅ | 全部完成 |
| 查询接口 | 3 | 3/3 | ✅ | 全部完成 |
| 数据类型 | 2 | 2/2 | ✅ | 全部完成 |
| 事务与存储过程 | 2 | 2/2 | ✅ | 全部完成 |
| 结果集消费 | 2 | 2/2 | ✅ | 全部完成 |
| 批量 DML | 3 | 3/3 | ✅ | 全部完成 |
| 批量导入 | 2 | 2/2 | ✅ | 全部完成 |
| 元数据查询 | 2 | 2/2 | ✅ | 全部完成 |
| 兼容数据库 | 1 | 1/1 | ✅ | 全部完成 |
| XCode 集成 | 1 | 1/1 | ✅ | 全部完成 |
| EF 集成 | 2 | 2/2 | ✅ | 全部完成 |

> **父需求状态判定**：所有子项三列均为 ✅ → ✅；任一子项非 ✅ → 🟡；所有子项均为 ❌ → ❌。
> 父需求完成度是**功能真正可用的唯一指标**。测试缺口集中在 SYS-3（WebAuthn）与 SYS-6（Unix Socket）两个平台/认证边界功能。
